---
description: Conventions for adding and customizing shadcn components
globs: src/components/ui/**/*.tsx
alwaysApply: false
---

# Component Conventions

## Folder structure

Each component lives in its OWN folder under `src/components/ui/<name>/` containing:

```
src/components/ui/button/
  button.tsx          # component code
  button.stories.tsx  # Storybook stories
  index.ts            # re-export: export * from './button'
```

Always import components via the alias `@/components/ui/<name>` (it resolves to the folder's `index.ts`), never the inner file path.

## Adding components

- Add new shadcn components with the CLI, never hand-create them:

```bash
npx shadcn@latest add <name>
```

- The CLI drops a flat `src/components/ui/<name>.tsx`. Move it into the folder layout above: create `ui/<name>/`, move the file to `ui/<name>/<name>.tsx`, and add `ui/<name>/index.ts` with `export * from './<name>'`.
- Keep all UI primitives under `src/components/ui/`.

## Writing components

- Use `cn()` from `@/lib/utils` for conditional/merged class names.
- Define variants with `cva` (class-variance-authority), mirroring the existing
  `button.tsx` pattern with `variant` + `size`:

```tsx
const buttonVariants = cva("base classes", {
  variants: { variant: { default: "...", outline: "..." } },
  defaultVariants: { variant: "default" },
})
```

- Style with theme tokens (`bg-primary`, `text-muted-foreground`, `border-border`).
  Never hardcode hex or oklch colors in a component - add a token in `src/index.css` instead.

## After changing a component

- Create or update its colocated `*.stories.tsx` so the change is visible in Storybook.
